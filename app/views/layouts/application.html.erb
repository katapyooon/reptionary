<!DOCTYPE html>
<html>
  <head>
    <title>Reptionary</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.5.13/cropper.min.css">
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true, type: "module" %>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
  </head>

  <body>
    <% if notice %><p class="notice"><%= notice %></p><% end %>
    <% if alert %><p class="alert"><%= alert %></p><% end %>
    
    <div class="header">
      <% if user_signed_in? %>
        <p>
          ログイン中: <%= current_user.id %>
          <%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete } %>
        </p>
      <% else %>
        <%= link_to "ログイン", new_user_session_path %>
      <% end %>
    </div>

    <main class="content">
      <%= yield %>
    </main>
    
    <footer class="site-footer">
      <div class="footer-content">
        <div class="footer-links">
          <%= link_to "利用規約", terms_path, class: "footer-link" %>
          <%= link_to "プライバシーポリシー", privacy_path, class: "footer-link" %>
          <%= link_to "お問い合わせ", inquiry_path, class: "footer-link" %>
          <span class="copyright">© <%= Time.current.year %> Reptionary. All rights reserved.</span>
        </div>
      </div>
    </footer>
  </body>
</html>